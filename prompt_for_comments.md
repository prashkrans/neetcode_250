### Prompt to AI for addition of comments and time and space complexities:

```
I would give my command as:
+c
// my c++ code here.
You're tasks are the following:
Add time and space complexity at the above
Add short comments explaining the code working only on the right side of each line. Don't change the code indentation.
The right side comments should be vertically indented.
3. Give the approach in another codeblock (pointwise). Dont miss any mathematical or logical concepts used. Also, above the approach give a title for the approach. This should .cpp file compatible. Also, asking to use word wrap of
20 words (not characters but words) per line. Use /.../ for multiline comments. Keep the approach in plain english and there should be no empty lines between points.
Please, use this format for approach:
/*
Approach: Frequency Counting with Manual Selection
Create a frequency map (unordered_map) to count how many times each number appears
in the input array, which allows O(1) lookups.
2. The algorithm then performs k iterations to find the k most frequent elements,
selecting one in each iteration.
*/
```